# AI
.claude

# Python
__pycache__/
*.pyc
.venv/
web-api/.venv/

# Env files (each app owns its own .env / .env.example)
.env

# Uploaded files (local dev; backed by a Docker volume in deployed environments)
web-api/uploads/
web-api/tests/.uploads-test/

# Node (web/ ships its own .gitignore for node_modules/dist, but cover it at the root too)
node_modules/
web/dist/
web/build/
