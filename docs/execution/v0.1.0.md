# Execution plan — v0.1.0

Ordered task breakdown for implementing v0.1.0, sequencing the work described in
[../architecture.md](../architecture.md) against the acceptance criteria in
[../requirements/v0.1.0.md](../requirements/v0.1.0.md). This doc tracks *what to build and in what
order*, not design decisions — those live in architecture.md.

## 1. Repo scaffolding

1. [x] Create `web/` and `web-api/` per architecture.md's [repository layout](../architecture.md#repository-layout-proposed).
   - [x] Init `web-api/` as a Python project (`pyproject.toml` or `requirements.txt`).
   - [x] Init `web/` as a Node project (Vite + React + TS template).
   - [x] Add `.gitignore` entries for both (venv/`__pycache__`, `node_modules`, build output).

## 2. Backend foundation

1. [x] Init FastAPI project structure in `web-api/`.
   - [x] `app/main.py` with the FastAPI app instance.
   - [x] Settings/config module reading env vars (e.g. `DATABASE_URL`).
   - [x] DB session dependency (SQLAlchemy engine + sessionmaker, FastAPI dependency for request-scoped sessions).
2. [x] SQLAlchemy model for `diary_entries`, `users`, and `sessions` per the [data model](../architecture.md#data-model).
   - [x] Declarative base + `DiaryEntry` model with typed columns (`id`, `title`, `content`, `entry_date`, `created_at`, `updated_at`).
   - [x] Add `user_id` FK column + relationship to `DiaryEntry`.
   - [x] `User` model (id, email [unique], hashed_password, created_at).
   - [x] `UserSession` model (id [opaque token, PK], user_id FK, created_at, expires_at).
3. [x] Alembic setup + initial migration.
   - [x] `alembic init`, wire `env.py` to the SQLAlchemy metadata and `DATABASE_URL`.
   - [x] Generate + review initial migration creating `users`, `diary_entries` (with `user_id` FK), and `sessions`.
4. [x] Pydantic request/response schemas.
   - [x] `DiaryEntryCreate` (title, content, entry_date).
   - [x] `DiaryEntryUpdate` (partial: title/content/entry_date).
   - [x] `DiaryEntryRead` (full entry incl. id/created_at/updated_at) and a lighter `DiaryEntrySummary` for list responses.
   - [x] `UserCreate`, `UserRead`, `UserLogin`.

## 3. Backend auth

1. [x] `app/security.py`.
   - [x] Password hashing (`pwdlib`, Argon2) — `hash_password` / `verify_password`.
   - [x] `create_session` / `delete_session` — opaque token stored in the `sessions` table.
   - [x] `get_current_user` dependency (reads the session cookie via `Request.cookies`) — 401 on missing/invalid/expired session.
2. [x] `app/routers/auth.py`.
   - [x] `POST /auth/register` — 201 + user profile; 409 on duplicate email.
   - [x] `POST /auth/login` — 200 + user profile; sets an `HttpOnly`/`SameSite=Lax` session cookie; 401 (generic) on bad credentials.
   - [x] `POST /auth/logout` — deletes the session server-side; clears the cookie.
3. [x] Wire `auth.router` into `app/main.py`.
4. [x] Add `CORSMiddleware` (`allow_credentials=True`, explicit `frontend_origin`) so the cookie flows across the frontend/backend ports.
5. [x] `GET /auth/me` — returns the current session's user (200 + `UserRead`) or 401 if not
   logged in. Needed by the frontend to restore auth state on page load and gate protected routes.

## 4. Backend CRUD + uploads

1. [x] `POST /diaries` — create.
   - [x] Reject missing title or entry_date (422).
   - [x] Default `entry_date` to today when omitted.
   - [x] Persist and return the created entry.
   - [x] Require auth; set `user_id` to the authenticated user.
2. [x] `GET /diaries` — list.
   - [x] Query ordered by `entry_date` desc.
   - [x] Return summary fields (id, title, entry_date).
   - [x] Require auth; scope to the authenticated user's own entries.
3. [x] `GET /diaries/{id}` — read.
   - [x] 404 when id doesn't exist.
   - [x] Return full entry (title, entry_date, content).
   - [x] Require auth; 404 (not 403) for another user's entry.
4. [x] `PUT /diaries/{id}` — update.
   - [x] 404 when id doesn't exist.
   - [x] Apply partial changes, refresh `updated_at`.
   - [x] Require auth; 404 for another user's entry.
5. [x] `DELETE /diaries/{id}` — delete.
   - [x] 404 when id doesn't exist.
   - [x] Remove the row (no orphan-image cleanup required, per requirements.md).
   - [x] Require auth; 404 for another user's entry.
6. [x] `POST /uploads/images` — upload.
   - [x] Accept multipart file, generate a unique filename, save under the uploads volume path.
   - [x] Return the public URL for the editor to embed.
   - [x] Require auth.
   - [x] Store under a per-user subdirectory (`uploads/{user_id}/...`); return `/media/{user_id}/...`.
7. [x] `GET /media/{user_id}/{filename}` — serve an uploaded image.
   - [x] Authenticated endpoint (not a bare static mount) — require a valid session.
   - [x] 404 (not 403) when the session's user doesn't match `{user_id}` or the file doesn't exist.

## 5. Backend tests

1. [x] Unit tests for business logic.
   - [x] Validation rules (title/entry_date required, defaulting behavior).
   - [x] `updated_at` refresh logic on edit.
2. [x] Integration tests against a real/test PostgreSQL instance.
   - [x] Test DB fixture (setup/teardown, isolated from dev DB).
   - [x] Full CRUD lifecycle test (create → list → get → update → delete).
   - [x] Upload + media test (file saved, URL returned, `GET /media/{user_id}/{filename}` serves it
     with the owner's session cookie).
   - [x] Auth: register → login (cookie set) → use the session to create/list/get/update/delete own diary entries.
   - [x] Auth: wrong-password login returns 401; duplicate-email register returns 409; missing/invalid/expired session on diary/upload/media endpoints returns 401; logout invalidates the session (subsequent requests with the same cookie 401).
   - [x] Cross-user isolation: user A cannot read/update/delete user B's entries, nor fetch user B's media (404, not 403).
3. [x] Write `web-api/README.md`.
   - [x] What the service is, how to run it locally (`uv run uvicorn ...`) and via Docker.
   - [x] Required env vars (`DATABASE_URL`, `SESSION_COOKIE_NAME`, `FRONTEND_ORIGIN`, etc.), referencing `web-api/.env.example`.
   - [x] How to run migrations (`alembic upgrade head`) and tests.

## 6. Frontend foundation

1. [ ] React + TypeScript scaffold in `web/`.
   - [ ] Routing setup (react-router) for list/detail/create/edit views, plus `/register` and `/login` routes.
   - [ ] Base project structure (`pages/`, `components/`, `api/`).
   - [ ] Auth state (e.g. React context) holding the current user; hydrated on load via `GET /auth/me`.
   - [ ] Protected-route wrapper: unauthenticated users are redirected to `/login` for all diary routes.
2. [ ] Tailwind CSS + shadcn/ui setup.
   - [ ] Install and configure Tailwind.
   - [ ] Init shadcn/ui, add base theme/components used across views (button, dialog, input).
3. [ ] Typed API client.
   - [ ] TS types mirroring the backend Pydantic schemas.
   - [ ] Fetch wrapper functions for each endpoint (auth register/login/logout/me, 5 diary, 1 upload).
   - [ ] All requests use `credentials: "include"` so the session cookie is sent/received —
     required now that auth is cookie-based rather than a bearer token the client attaches itself.

## 7. Frontend editor

1. [ ] Tiptap integration.
   - [ ] Install Tiptap + StarterKit (headings, lists incl. task lists, blockquote, code blocks, links).
   - [ ] Add GFM tables + strikethrough extension.
   - [ ] Add text-style/color extension.
   - [ ] Add image extension.
2. [ ] Markdown serialization via `tiptap-markdown`.
   - [ ] Editor content → markdown string on save.
   - [ ] Markdown string → editor content on load (existing entry edit).
   - [ ] Confirm non-markdown formatting (text color) round-trips as inline HTML.

## 8. Frontend flows

Mapped to requirements/v0.1.0.md's functional requirements 1–8:

1. [ ] Register flow.
   - [ ] Register form: email + password inputs, client-side validation (required, password min length).
   - [ ] On success, redirect to `/login` (register does not auto-login, per requirements.md).
   - [ ] Duplicate-email (409) shows a clear error without confirming other emails exist.
2. [ ] Login flow.
   - [ ] Login form: email + password inputs.
   - [ ] On success, store the returned user in auth state and redirect to the diary list.
   - [ ] Incorrect credentials (401) show one generic error.
   - [ ] Logout action (e.g. nav button) calls `POST /auth/logout`, clears auth state, redirects to `/login`.
3. [ ] Create entry.
   - [ ] Create form: title input, entry-date picker (default today), editor for content.
   - [ ] Client-side validation blocking save without title/date.
   - [ ] On success, navigate to/refresh list so the new entry is immediately visible.
4. [ ] List entries.
   - [ ] List page fetching `GET /diaries`.
   - [ ] Render title + entry date per row, chronological order preserved from API.
5. [ ] View single entry.
   - [ ] Detail page fetching `GET /diaries/{id}`.
   - [ ] Render title, entry date, and markdown content.
6. [ ] Edit entry.
   - [ ] Edit form pre-filled from `GET /diaries/{id}`.
   - [ ] Save calls `PUT /diaries/{id}`; list re-sorts if entry_date changed.
7. [ ] Insert image.
   - [ ] Wire editor's upload button to `POST /uploads/images`.
   - [ ] Wire drag-and-drop into the editor to the same upload call.
   - [ ] Insert returned URL as an inline image in the content.
8. [ ] Delete entry.
   - [ ] Delete action opens a confirmation dialog.
   - [ ] On confirm, call `DELETE /diaries/{id}` and remove the entry from the list view.

## 9. Frontend tests

1. [ ] Vitest + React Testing Library unit/component tests.
   - [ ] Form validation (create/edit) blocks missing title/date.
   - [ ] List renders entries in the order the API returns them.
   - [ ] Delete confirmation dialog gates the delete call.
   - [ ] Register/login form validation and error display (duplicate email, bad credentials).
   - [ ] Protected routes redirect unauthenticated users to `/login`.
2. [ ] Playwright e2e.
   - [ ] Full CRUD flow through the UI: create → appears in list → view → edit → delete.
   - [ ] Image insert flow: upload/drag-drop an image, verify it renders in the saved entry.
   - [ ] Auth flow: register → login → diary CRUD works → logout → protected routes redirect to login.
3. [ ] Replace the Vite scaffold placeholder in `web/README.md` with real project docs.
   - [ ] What the app is, how to run it locally (`pnpm install && pnpm dev`) and via Docker.
   - [ ] Required env vars, referencing `web/.env.example`.
   - [ ] How to run unit tests (Vitest) and e2e tests (Playwright).

## 10. Containerization

1. [ ] `Dockerfile` for frontend.
   - [ ] Multi-stage build: install/build, then serve static output.
2. [ ] `Dockerfile` for backend.
   - [ ] Python base image, install deps, run via uvicorn.
3. [ ] Root `docker-compose.yml`.
   - [ ] Services: `web`, `web-api`, `postgres`. (`postgres` already stood up early — see root
     `docker-compose.yml` — to unblock local backend dev/testing ahead of Tasks 4/5; `web` and
     `web-api` get added here once their Dockerfiles exist.)
   - [x] Named volume for Postgres data.
   - [ ] Named volume for uploaded images, mounted into `web-api`.
4. [ ] Env var config.
   - [ ] `DATABASE_URL` and `FRONTEND_ORIGIN` wired from compose into the backend service.
   - [ ] Each app owns its own `.env`/`.env.example` (`web-api/.env.example` already added in
     Task 2; add `web/.env.example` here); compose wires them in per-service via `env_file:`
     rather than a single shared root file.

## 11. Final acceptance walkthrough

1. [ ] `docker compose up` brings up frontend, backend, and database, ready to use.
   - [ ] All three containers report healthy/ready.
   - [ ] Migrations apply automatically or via a documented one-off command.
2. [ ] Through the UI: register an account, log in, create/list/view/edit/delete diary entries,
      and log out, end-to-end.
   - [ ] Manual pass through all 8 functional requirements from requirements.md.
   - [ ] Confirm data survives a container restart (`docker compose down && up`).

## 12. Release docs

1. [ ] Write the root `README.md`.
   - [ ] What Living Genie is (brief, from requirements.md's overview).
   - [ ] How to run the whole stack via `docker compose up`.
   - [ ] Links to `docs/architecture.md`, `docs/requirements/v0.1.0.md`, and `docs/roadmap.md`.
2. [ ] Write the root `CHANGELOG.md`.
   - [ ] Follow a standard format (e.g. Keep a Changelog).
   - [ ] Add a `v0.1.0` entry summarizing what shipped (registration/login, diary CRUD, image
     uploads, containerized frontend/backend/DB stack), sourced from requirements/v0.1.0.md's
     functional requirements.
3. [ ] Write `docs/release-notes/v0.1.0.md`.
   - [ ] User-facing summary of what's new in this release.
   - [ ] Link back to requirements/v0.1.0.md and roadmap.md for full detail.
